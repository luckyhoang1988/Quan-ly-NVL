# NVL/WMS — MẪU biến môi trường.
# Copy file này thành .env rồi điền giá trị thật:  cp .env.example .env
# Sinh SECRET_KEY:  python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

SECRET_KEY=thay-bang-secret-key-that
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# --- PostgreSQL ---
DB_ENGINE=django.db.backends.postgresql
DB_NAME=ql-nvl
DB_USER=qlnvl
DB_PASSWORD=mat-khau-postgres-cua-ban
DB_HOST=127.0.0.1
DB_PORT=5432
